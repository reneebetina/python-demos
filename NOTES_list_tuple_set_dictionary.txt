STRING - text ; IMMUTABLE

https://www.educative.io/answers/list-vs-tuple-vs-set-vs-dictionary-in-python

LIST is a collection of ordered data.	MUTABLE
  append()
  sorted()
  reverse()
  pop()
  Search using the index

TUPLE is an ordered collection of data. IMMUTABLE-cannot be changed
  CANNOT BE SORTED
  CANNOT BE REVERSED
  The order matters in a tuple

SET is an unordered collection.	MUTABLE, NO DUPLICATE ITEMS
  add()
  CANNOT BE SORTED
  CANNOT BE REVERSED
  
DICTIONARY is an unordered collection of data that stores data in key-value pairs. 	MUTABLE, NO DUPLICATE ITEMS
  push()
  pop()
  update()
  sort()
  search / get value using the key
  CANNOT BE REVERSED - but you may sort then get first/last element